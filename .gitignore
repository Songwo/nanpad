# dependencies
node_modules/
.node_modules.lock

# build output
dist/
.output/
.nitro/
.vercel/
.tanstack/

# local env — the platform injects these on deploy, never commit them
.env
.env.*
!.env.example

# editor / OS
.DS_Store
Thumbs.db
*.log

# local dev-server launcher (machine specific)
.claude/
dist-desktop/
release/

# 含个人资料的旧截图仅保留在本机，不再进入公开仓库。
/attachments/image.png
/screenshots/oauth-panel.png
/screenshots/agent-final.png
/screenshots/mail-login.png
