# v0.11.0 安全加固与质量改进

状态：进行中。目标版本 0.11.0。本文基于 0.10.0 的全面审查（2026-09-14，四路并行审查：前端质量、Electron 安全、测试工程化、服务端数据层）编写，引用处均已核实。

## 审查基线

`typecheck` / `lint` / `i18n:check` / `npm test` 在修复 Playwright 环境后全绿（479 项）。lint 有 9 个既有小警告（fast-refresh 导出 6 个、无用 eslint-disable 1 个、多余依赖 1 个），非阻塞。

## 已完成（本次提交）

| # | 改进 | 内容 | 验证 |
| --- | --- | --- | --- |
| 1 | **SSH 主机密钥 TOFU 验证（P0）** | 首次握手将主机指纹（OpenSSH 格式 `SHA256:base64`）存入主密码库 `ssh-host:<host>:<port>`；后续连接强制比对，不匹配即拒绝并展示新旧指纹。指纹记录走 GCM 认证标签，落盘被篡改会直接解密失败。锁库时 fail-closed（拒绝连接而非放行）。误装服务器后可在资产 SSH 凭据面板「重置指纹并重试」。`assertPublicVaultRecord` 阻止渲染进程直接读写 `ssh-host:` 记录 | 新增 `electron/services/ssh.test.mjs` 2 项测试（TOFU 全周期、存储不可用 fail-closed）；既有 SFTP 测试改为经过验证路径后仍通过 |
| 2 | **Electron 渲染进程加固** | `sandbox: true`（preload 仅用 contextBridge/ipcRenderer，沙箱兼容）；新增 `will-navigate` 拦截（生产锁死单页，开发仅放行 Vite 同源，外部链接转系统浏览器）；生产菜单移除 DevTools 入口 | typecheck 通过；菜单构造无异常 |
| 3 | **GitHub Actions CI** | `.github/workflows/ci.yml`：Node 22 + Playwright 浏览器缓存，跑 typecheck / lint / i18n:check / test / build（`db:migrate` 无 DATABASE_URL 时自动退出，无需密钥）；`check:auth` 需要探测运行中的 dev server，CI 里先启动 `npm run dev` 再校验并随步骤关闭（兼作冷启动冒烟） | 首跑暴露 check:auth 的 live-server 依赖并已修复；本地起 dev server 后 check:auth 通过 |
| 4 | **.gitignore 修复** | 未跟踪噪音从 130 个降到 0：`screenshots/*` 全忽略 + `nanpad-v0*`/`.gitkeep` 白名单（已跟踪的 19 张展示图不受影响，新增展示图用 `git add -f`）；`scratchpad-*.json`、`attachments/` 忽略 | `git status` 仅剩本次新增文件 |
| 5 | **Playwright 开箱体验** | README 开发章节补 `npx playwright install chromium` 说明（含国内镜像变量）；`package.json` 加 `engines.node >= 22.6`（`--experimental-strip-types` 的下限） | 本机镜像安装实测分钟级完成 |
| 6 | **修复两个已腐烂的集成脚本**（P3 #12 的两个活例，均经 stash 基线对比确认与本次改动无关） | `desktop-integration.mjs`：指标样本注入挪到应用首次 `metrics.list` 之前——0.9.0 起 `MetricsStore` 解析结果常驻内存（`metrics.mjs:24`），进程缓存后看不到外部写入；`agent-integration.mjs`：等待文案改为 `/已加载 \d+ 个模型/`——0.9.0 模型设置改版把「模型列表已更新」改成了带数量的提示（`agent-settings.tsx:189`） | 两脚本均 exit 0、无页面错误 |

## 待办清单（按优先级）

### P1 — 重要工程缺口

| # | 事项 | 位置 / 证据 | 建议 |
| --- | --- | --- | --- |
| 1 | Better Auth「配置即 404」：`/api/auth/*` 无任何 HTTP 挂载点（全仓库无 `auth.handler`/catch-all 路由，构建产物无 better-auth 痕迹），`VITE_AUTH_ENABLED=true` 部署时登录链路断裂 | `src/lib/auth/server.ts:7` | 决策：纯桌面定位则整体裁剪 `src/lib/auth/` 服务器分支与 PG 基建（`migrations/`、`db.ts`、`db:migrate`）；要保留 Web 登录则补挂载点。业务数据 100% 本地 JSON，数据库层目前只服务 auth |
| 2 | `migrations/auth/0001_auth.sql` 永远不会被自动应用（两个执行器都不递归子目录），依赖「开登录时手工复制」的未文档化契约 | `scripts/migrate.mjs:34`、`src/lib/db.ts:140` | 随 #1 一并决策；若保留则加启动告警或并入根目录 |
| 3 | vault 密码学与策略：scrypt `N=2^15` 低于 OWASP 现行建议（≥2^16，建议 2^17）；主密码下限不一致（`vault.mjs` 6 位 vs `profile.mjs` 10 位）；无空闲自动锁定（托盘常驻 + 解锁态无限期保留） | `electron/services/vault.mjs:19,106` | scrypt 升级需在文件头记录参数并支持渐进迁移（老库读旧参数、改密时升档）；空闲锁定用 `powerMonitor` + 可配置超时 |
| 4 | OAuth 模块零测试（230 行 token 交换/回调，安全敏感） | `electron/services/oauth.mjs` | 仿照 `agent-integration.mjs` 的本地 mock server 模式补单测 |

### P2 — 代码质量

| # | 事项 | 位置 / 证据 |
| --- | --- | --- |
| 5 | 上帝组件拆分：`mail-reader.tsx`（1081 行，ReaderBody 20 个 useState）、`views.tsx`（888 行 13 个组件）、`composer.tsx`（679）、`operations-panel.tsx`（589，五个不相关组件的杂物箱）、`ai-accounts.tsx`（615）、`settings.tsx`（541） | 对应文件 |
| 6 | `FolderEditor` 两份 ~100 行近乎逐行相同 | `mail-workspace.tsx:299`、`vault-workspace.tsx:284` |
| 7 | 20+ 处 busy/error/try-catch 样板，应封装 `useAsyncTask` + `errorText()` | `agent-settings.tsx:21` 等全局分布 |
| 8 | 幽灵依赖：`@tanstack/react-query`、`react-hook-form`、`@hookform/resolvers`、`accordion`/`radio-group`/`slider` 等 7+ 包声明未用；同时三处手写 cancelled/revision 轮询正是 react-query 强项 | `package.json`、`operations-panel.tsx:183`、`ai-accounts.tsx:269`、`mail-reader.tsx:176` |
| 9 | 分层反向依赖：`src/` 5 处 import `electron/services/image-data.mjs`，2 处 import `scripts/*.mjs` | `src/lib/store.ts:22`、`src/lib/db.ts:1` 等 |
| 10 | `desktop()!` 非空断言 16 处，`mail-reader.tsx:143` 组件顶层断言在浏览器环境直接崩 | 全局分布 |
| 11 | `mail-reader.tsx:136` `"未分组"` 未包 `t()`，英文界面漏中文 | 单处 |

### P3 — 测试与工程化

| # | 事项 | 位置 / 证据 |
| --- | --- | --- |
| 12 | 11 个 `*integration.mjs` 全 mock、无真实凭证、目录隔离，但没接进 `npm test` 也没进 CI，会随代码漂移腐烂 | `scripts/agent-integration.mjs` 等 |
| 13 | test glob 不递归（`scripts/*.test.mjs`），子目录测试静默漏跑 | `package.json:20` |
| 14 | 无 pre-commit 钩子；eslint 全局关闭 `no-explicit-any` | `eslint.config.mjs:44` |
| 15 | CI 目前只跑 ubuntu；发布产物是 Windows 安装包，可考虑加 windows job（成本换覆盖） | `.github/workflows/ci.yml` |

### P4 — 低优先级

| # | 事项 | 位置 / 证据 |
| --- | --- | --- |
| 16 | 扩展「自动采集」默认开启是激进默认（0.10.0 有意为之，需产品决策是否 opt-in / 首次配对明确同意 / 站点白名单） | `browser-extension/manifest.json`、`submit-capture.mjs:9` |
| 17 | CSP 收窄（`style-src 'unsafe-inline'`、`img-src http:` 全开、缺 `object-src`/`base-uri`/`frame-ancestors`）；打包版仍从 Google Fonts 拉远程字体 | `electron/renderer/index.html:9` |
| 18 | 扩展 `sentKeys` 以明文账号密码为键驻留内存，建议单向哈希；popup.html 补显式 CSP | `submit-capture.mjs:99` |
| 19 | 本地桥固定端口 47832 是指纹，可改随机端口 + 配对码携带 | `extension-bridge.mjs:295` |
| 20 | `handle()` 直传底层 `err.message`，可白名单化；`app:info` 暴露 userData 绝对路径 | `electron/main.mjs` |
| 21 | 文档卫生：归档 `docs/RELEASE-0.2.0.md`/`VERIFICATION-0.2.0.md`；README 结构表补 `browser-extension/`；`AGENTS.md` 是 Grok 脚手架文档与项目无关，应替换为 Nanpad 的开发约定 | 对应文件 |
| 22 | 依赖主版本可升：react-table 9、lucide-react 1.x、react-day-picker 10、eslint 10、imapflow 2 等 | `npm outdated` |
| 23 | lint 9 个既有警告（fast-refresh 等） | 见基线 |

## 发布提醒

- 本次改动需要在 `src/lib/changelog.ts` 增加条目（SSH 主机指纹校验、渲染进程加固），发布流程见 `docs/RELEASING.md`。
- 存量用户升级后首次 SSH 连接会重新走一次 TOFU 记录（旧数据无指纹，不产生破坏性变更）。
