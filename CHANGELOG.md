# 更新日志

> 本文件由 `node scripts/write-changelog.mjs` 从 `src/lib/changelog.ts` 生成，
> 请改那个文件而不是这里 —— 应用内的「设置 → 更新日志」读的是同一份数据。

## 0.5.0 — 邮箱文件夹、邮件阅读与快速发信

*2026-09-09*

- 邮箱页默认按工作、生活和自定义文件夹收纳，支持重命名、颜色、批量移动和删除分组。
- 新增 IMAP 邮件阅读，支持服务端文件夹、分页、主题或发件人搜索、未读筛选与显式标记已读。
- 正文在本机转为纯文本，附件按需下载，不加载远程图片，也不发送给 Agent。
- 新增 SMTP 写信、回复和文本转发，支持抄送、密送、附件及发送前确认。
- 邮件草稿使用主密码库加密保存；锁库取消邮件操作并清除阅读区中的正文与草稿。
- 发信强制 TLS 或 STARTTLS；结果不明确时不自动重发，部分收件人被拒绝时保留草稿。

## 0.4.0 — 浏览器快速保存、批量整理与多选关联

*2026-09-09*

- 新增 Chrome / Edge 浏览器扩展，发送当前站点到司南，确认后填写账号并加密保存。
- 网站账号保存时可直接关联注册邮箱；外部请求排队，不覆盖正在填写的表单。
- 新增批量整理，支持筛选、多选资产、添加或移除标签，以及关联到同一资产。
- 资产详情支持搜索与多选关联；关系图开启连接模式后可拖动端点建立关联。
- 插件仅申请当前标签页权限，不读取密码、Cookie 或历史记录；跨应用链接只保留站点根地址。

## 0.3.0 — 网站账号、实时邮箱与私密邮件提醒

*2026-09-09*

- 支持保存网站登录账号并关联注册邮箱，取消解锁或凭据保存失败时保留表单。
- 邮箱保存独立 IMAP 连接配置，实时检查收件箱总数、未读与本次新增。
- Agent 支持凭据位置引用；本次明确允许后可查询已保存邮箱，不读取密码。
- 新增 Telegram、Server酱和企业微信邮件提醒，默认关闭，仅发送数量和检查时间。
- 邮件推送独立记录检查基线与失败重试，首次检查不推送历史邮件，锁库即取消在途任务。
- 推送检查进度与待发提醒一并加密保存，避免查到新邮件后锁库导致漏提醒。
- 密钥库统一串行保存、解锁和换主密码，修复后台推送与手动保存凭据的并发冲突。

## 0.2.2 — 登录回调入口与授权状态提示

*2026-09-08*

- 等待登录授权时直接显示回调链接或授权码输入框，无需展开。
- 分别显示等待回调、验证授权和同步额度，已保存账号单独标明授权状态。
- 区分额度同步失败与缺少回调，HTTP 403 不再推断具体拒绝原因。
- 统一 HTTP 403 与 429 的通用错误提示，避免将令牌请求错误标为额度查询失败。
- 补充自动回调、旧链接失效及额度查询失败的操作说明。

## 0.2.1 — 授权回调、分类额度与自定义图片

*2026-09-08*

- 添加 AI 订阅默认进入快速登录，四家授权成功后自动关联订阅。
- 支持粘贴完整回调链接或授权码，回调端口占用时可手动完成授权。
- 补充模型与产品分类额度、剩余数量和重置时间，明确额度来源及网页权益的读取范围。
- 查询失败保留上次成功数据并显示失败状态，不将缺失额度当作零用量。
- 统一下拉菜单的主题、键盘操作、选中状态及长文本展示。
- 个人资料支持头像，资产支持自定义图片；上传后本地压缩保存，刷新授权保留图片。

## 0.2.0 — 个人空间与 AI 服务账号

*2026-09-07*

- 首次使用设置姓名与主密码，升级时保留原有资产和加密凭据。
- 模型回答、流式输出与知识库引用支持 Markdown、表格、列表和代码块。
- OpenAI、Claude、Grok、Gemini 分别支持网页登录授权、本机加密令牌及用量查询。
- 新增模型服务商预设；账号授权与 OpenAI 兼容模型调用配置独立管理。
- 生产设置移除演示数据入口，补充安装、使用、AI 授权与安全文档。

## 0.1.0 — 第一个可用版本

*2026-09-04*

- 中英文切换与跟随系统语言，日期和数字格式同步更新。
- 托盘常驻、到期和离线系统通知，可在外观设置中开关。
- 真实指标历史：保留最近 30 天，查看 CPU、内存与磁盘曲线。
- SFTP 目录浏览、筛选和文件下载，复用加密的 SSH 凭据。
- 资产双向关联、解除与详情跳转，随资产数据持久化。
- 需要留意事项导出为 ICS 日历，不包含凭据与备注。
- 设置面板：主题（跟随系统 / 浅色 / 深色）、修改主密码、数据目录、版本与更新检查、更新日志。
- 深色主题：表面按层级抬升而不是简单反色，阴影换成发丝描边，状态色与滚动条同步适配。
- 窗口标题栏由应用自绘：整条都可拖拽，最小化 / 最大化 / 关闭与侧栏共用一套 hover。
- 跨分类「分组」视图：一个标签看到它下面所有类型的资产，右栏与 ⌘K 都能跳进来。
- 六类资产通用的标签与分组：标签条多选取交集，可切换平铺 / 按标签分节。
- 每类资产都能附加密的账号密码：登录地址、账号、密码、备注，详情页一键复制。
- 真实 SSH 终端（xterm + PTY）与主机指标采集（CPU / 内存 / 磁盘 / 负载 / 内核）。
- 真实 WHOIS 与 DNS 查询、真实 TLS 握手读取证书，过期与不受信任会单独标红。
- scrypt + AES-256-GCM 凭据库；密钥完整值不再明文存放，首次解锁自动搬迁。
- 三栏桌面布局与整套动效：FLIP 卡片放大、滑动标签下划线、视图切换入场、数字滚动。
